package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.PageFactory;

public class ShelfLoginPage
{
//    @FindBy (name = "email") WebElement emailField;
//    @FindBy (name = "password")  WebElement passField;
//    @FindBy (className = "sc-ieecCq elIJHX") WebElement signIn;

    WebDriver driver;

    public ShelfLoginPage(WebDriver driver) {
        this.driver = driver;
        PageFactory.initElements(driver, this);
    }

    private void loginPage() { driver.get("http://localhost:3000/login"); }
    private void enterValidCredentials()
    {
        WebElement emailField = driver.findElement(By.name("email"));
        emailField.sendKeys("srdjan.rados@htecgroup.com");
        WebElement passField = driver.findElement(By.name("password"));
        passField.sendKeys("Qwertysha1@");
    }
    private void clickSignIn()
    {
        WebElement signIn = driver.findElement(By.className("sc-ieecCq elIJHX"));
        WebElement clickSign = waitToBeClickable(signIn);
        clickSign.click();
    }
    public void login()
    {
        loginPage();
        enterValidCredentials();
        clickSignIn();
    }
}

package tests;

import org.junit.Test;
import pages.ShelfLoginPage;

public class ShelfLoginTest extends BaseTest
{
    @Test
    public void loginPage()
    {

        ShelfLoginPage loginSP = new ShelfLoginPage(driver);
        loginSP.login();
    }

}






